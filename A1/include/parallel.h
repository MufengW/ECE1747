#pragma once
#include <iostream>
#include <vector>
#include <thread>
#include "global.h"

extern GlobalConfig g_config;
extern GlobalData g_data;

void parallel_threading();
void parallel_processing();
